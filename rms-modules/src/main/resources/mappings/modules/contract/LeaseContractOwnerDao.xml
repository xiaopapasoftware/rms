<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.contract.dao.LeaseContractOwnerDao">

	<sql id="sqlColumns">
		a.ID AS "id",
		a.LEASE_CONTRACT_ID AS "leaseContractId",
		a.OWNER_ID AS "ownerId",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS
		"updateBy.id",
		a.update_date AS
		"updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>


	<select id="get" resultType="LeaseContractOwner">
		SELECT
		<include refid="sqlColumns" />
		FROM t_lease_contract_owner a
		WHERE a.id = #{id}
	</select>

	<select id="findList" resultType="LeaseContractOwner">
		SELECT
		<include refid="sqlColumns" />
		FROM t_lease_contract_owner a
		<where>
			a.del_flag = '0'
			<if test="leaseContractId != null and leaseContractId != ''">
				AND a.LEASE_CONTRACT_ID = #{leaseContractId}
			</if>
			<if test="ownerId != null and ownerId != ''">
				AND a.OWNER_ID = #{ownerId}
			</if>
		</where>

		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>



	<insert id="insert">
		INSERT INTO t_lease_contract_owner(
		ID,
		LEASE_CONTRACT_ID,
		OWNER_ID,
		create_by,
		create_date,
		update_by,
		update_date,
		remarks,
		del_flag
		) VALUES (
		#{id},
		#{leaseContractId},
		#{ownerId},
		#{createBy.id},
		#{createDate},
		#{updateBy.id},
		#{updateDate},
		#{remarks},
		#{delFlag}
		)
	</insert>

	<update id="update">
		UPDATE t_lease_contract_owner SET
		LEASE_CONTRACT_ID = #{leaseContractId},
		OWNER_ID = #{ownerId},
		update_by = #{updateBy.id},
		update_date = #{updateDate}
		WHERE id = #{id}
	</update>

	<update id="delete">
		UPDATE t_lease_contract_owner SET
		del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>

	<select id="getListByContractId" resultType="LeaseContractOwner">
		SELECT
		<include refid="sqlColumns" />
		FROM t_lease_contract_owner a
		where
			a.del_flag = '0'
			AND a.LEASE_CONTRACT_ID = #{leaseContractId}
	</select>

	<delete id="deleteListByContractId">
		delete FROM t_lease_contract_owner
		WHERE LEASE_CONTRACT_ID = #{leaseContractId}
	</delete>

	<select id="getContractIdListByOwnerIdList" resultType="String">
		SELECT LEASE_CONTRACT_ID
		FROM t_lease_contract_owner
		where
		del_flag = '0'
		AND OWNER_ID in
		<foreach collection="ownerIdList" item="ownerId" open="(" close=")" separator=",">
			 #{ownerId}
		</foreach>
	</select>

</mapper>