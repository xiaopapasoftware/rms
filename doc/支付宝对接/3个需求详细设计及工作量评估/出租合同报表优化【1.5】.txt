1、把报表管理下菜单名称为 “合同报表”的名字改为“出租合同统计”
2、报表的查询条件里新增：区县、服务中心、营运区域
3、报表的查询结果和导出结果里都新增一个列： ”对应房间数“。如果是整租合同，对应房间数为该整租合同对应房屋下所包含的单间数；如果是单间合租类型合同，则该字段值为1。
4、在出租合同报表的查询结果的最后一行和导出的excel文件的最后一行，分别加上当前查询结果或导出结果的汇总合同数量和，以及如果某一列是金额（例如房租、押金、电费等）同时汇总出该列的总金额值，放在最后一行。